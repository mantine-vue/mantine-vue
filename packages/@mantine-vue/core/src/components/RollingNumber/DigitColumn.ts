import DigitColumnComponent from './DigitColumn.vue'

/** One rolling digit of a `RollingNumber`, animated by translating a strip of cells. */
export const DigitColumn = DigitColumnComponent

export type { DigitColumnProps } from './DigitColumn.types'
