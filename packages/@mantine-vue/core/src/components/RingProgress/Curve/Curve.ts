import CurveComponent from './Curve.vue'

/** A single arc of a `RingProgress`, or the background ring when `root` is set. */
export const Curve = CurveComponent

export type { RingProgressCurveOwnProps, RingProgressCurveProps } from './Curve.types'
