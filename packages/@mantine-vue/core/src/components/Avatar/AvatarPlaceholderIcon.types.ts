import type { SVGAttributes } from 'vue'

/** Native SVG attributes accepted by the avatar placeholder icon. */
export interface AvatarPlaceholderIconProps extends SVGAttributes {
  /** Unique identifier assigned to the SVG element. */
  id?: string
}
