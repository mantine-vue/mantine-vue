import ScrollAreaScrollbarComponent from './ScrollAreaScrollbar.vue'

/** Renders a scrollbar unless the scroll area is configured to never show one. */
export const ScrollAreaScrollbar = ScrollAreaScrollbarComponent

export type {
  ScrollAreaScrollbarProps,
  ScrollAreaScrollbarSlots,
  ScrollAreaScrollbarVisibleProps,
} from './ScrollAreaScrollbar.types'
