import ScrollAreaScrollbarXComponent from './ScrollbarX.vue'

/** X-axis scrollbar: measures the horizontal dimension and drives the viewport scroll. */
export const ScrollAreaScrollbarX = ScrollAreaScrollbarXComponent
