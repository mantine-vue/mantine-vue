import ScrollAreaScrollbarVisibleComponent from './ScrollAreaScrollbarVisible.vue'

/**
 * Owns the measured sizes and the thumb element, and routes to the horizontal or
 * vertical scrollbar implementation.
 */
export const ScrollAreaScrollbarVisible = ScrollAreaScrollbarVisibleComponent

export type { ScrollAreaScrollbarVisibleProps } from './ScrollAreaScrollbar.types'
