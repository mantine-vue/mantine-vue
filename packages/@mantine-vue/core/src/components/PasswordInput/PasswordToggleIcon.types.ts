/** Props declared by `PasswordToggleIcon` itself. */
export interface PasswordToggleIconProps {
  /**
   * If set, the "hide password" icon is rendered instead of the "show password" one.
   *
   * @default false
   */
  reveal?: boolean
}
