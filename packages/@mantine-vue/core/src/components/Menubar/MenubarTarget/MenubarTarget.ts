import MenubarTargetComponent from './MenubarTarget.vue'

/** Button that opens one menu of a `Menubar` and carries its keyboard model. */
export const MenubarTarget = MenubarTargetComponent

export type {
  MenubarTargetOwnProps,
  MenubarTargetProps,
  MenubarTargetSlots,
} from './MenubarTarget.types'
