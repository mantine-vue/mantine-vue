import MenubarMenuComponent from './MenubarMenu.vue'

/** A single menu of a `Menubar`, wrapping `Menu` with the menubar's shared state. */
export const MenubarMenu = MenubarMenuComponent

export type { MenubarMenuProps, MenubarMenuSlots } from './MenubarMenu.types'
