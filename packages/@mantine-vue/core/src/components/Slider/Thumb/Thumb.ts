import ThumbComponent from './Thumb.vue'

/** Draggable thumb of a `Slider` or `RangeSlider`, including its floating label. */
export const Thumb = ThumbComponent

export type { SliderThumbOwnProps, SliderThumbProps, SliderThumbSlots } from './Thumb.types'
