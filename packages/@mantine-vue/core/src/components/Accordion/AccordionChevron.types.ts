/** Props declared by `AccordionChevron` itself. See `AccordionChevronProps` for the full public type. */
export interface AccordionChevronOwnProps {
  /**
   * Controls `width` and `height` of the icon, `16` by default
   *
   * @default 16
   */
  size?: number | string
}

export type AccordionChevronProps = AccordionChevronOwnProps
