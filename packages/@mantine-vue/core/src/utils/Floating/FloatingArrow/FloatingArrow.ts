import FloatingArrowComponent from './FloatingArrow.vue'

/** Arrow rendered by the floating components (Popover, Tooltip, HoverCard, Menu). */
export const FloatingArrow = FloatingArrowComponent

export type { FloatingArrowProps } from './FloatingArrow.types'
