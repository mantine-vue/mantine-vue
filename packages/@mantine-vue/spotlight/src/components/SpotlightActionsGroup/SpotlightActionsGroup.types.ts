export type SpotlightActionsGroupStylesNames = 'actionsGroup'
/** Props accepted by `SpotlightActionsGroup`. */
export interface SpotlightActionsGroupProps {
  /** Group label. */
  label?: string
  /** CSS classes applied to component elements. */
  classNames?: Record<string, any>
  /** Inline styles applied to component elements. */
  styles?: Record<string, any>
  /** CSS variables applied to component elements. */
  vars?: Record<string, any>
  [key: string]: any
}
