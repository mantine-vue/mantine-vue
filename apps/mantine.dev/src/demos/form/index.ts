import { usage } from './Form.demo.usage'
import { validation } from './Form.demo.validation'
import { status } from './Form.demo.status'
import { transformValues } from './Form.demo.transformValues'
import { useFieldDemo } from './Form.demo.useField'

export const FormDemos = {
  usage,
  validation,
  status,
  transformValues,
  useField: useFieldDemo,
}
