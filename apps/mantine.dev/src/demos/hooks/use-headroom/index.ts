import { usage } from './use-headroom.demo.usage'
import { scrollProgress } from './use-headroom.demo.scrollProgress'
import { callbacks } from './use-headroom.demo.callbacks'

export const UseHeadroomDemos = {
  usage,
  scrollProgress,
  callbacks,
}
