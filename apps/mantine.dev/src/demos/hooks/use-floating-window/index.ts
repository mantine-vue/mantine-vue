import { usage } from './use-floating-window.demo.usage'
import { constrainToViewport } from './use-floating-window.demo.constrainToViewport'
import { constrainOffset } from './use-floating-window.demo.constrainOffset'
import { dragHandleSelector } from './use-floating-window.demo.dragHandleSelector'
import { enabled } from './use-floating-window.demo.enabled'
import { setPosition } from './use-floating-window.demo.setPosition'
import { axis } from './use-floating-window.demo.axis'

export const UseFloatingWindowDemos = {
  usage,
  constrainToViewport,
  constrainOffset,
  dragHandleSelector,
  enabled,
  setPosition,
  axis,
}
