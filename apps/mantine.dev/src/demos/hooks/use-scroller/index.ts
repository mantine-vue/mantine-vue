import { usage } from './use-scroller.demo.usage'
import { scrollAmount } from './use-scroller.demo.scrollAmount'
import { draggable } from './use-scroller.demo.draggable'

export const UseScrollerDemos = {
  usage,
  scrollAmount,
  draggable,
}
