import { usage } from './use-mask.demo.usage'
import { complete } from './use-mask.demo.complete'
import { dynamic } from './use-mask.demo.dynamic'
import { customTokens } from './use-mask.demo.custom-tokens'
import { escape } from './use-mask.demo.escape'
import { regex } from './use-mask.demo.regex'
import { reset } from './use-mask.demo.reset'

export const UseMaskDemos = {
  usage,
  complete,
  dynamic,
  customTokens,
  escape,
  regex,
  reset,
}
