import { usage } from './Badge.demo.usage'
import { gradient } from './Badge.demo.gradient'
import { rounded } from './Badge.demo.rounded'
import { sections } from './Badge.demo.sections'
import { fullWidth } from './Badge.demo.fullWidth'
import { autoContrast } from './Badge.demo.autoContrast'
import { variantColorsResolver } from './Badge.demo.variantColorsResolver'

export const BadgeDemos = {
  usage,
  gradient,
  rounded,
  sections,
  fullWidth,
  autoContrast,
  variantColorsResolver,
}
