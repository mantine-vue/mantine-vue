import { segmented } from './FloatingIndicator.demo.segmented'
import { direction } from './FloatingIndicator.demo.direction'
import { tabs } from './FloatingIndicator.demo.tabs'

export const FloatingIndicatorDemos = {
  segmented,
  direction,
  tabs,
}
