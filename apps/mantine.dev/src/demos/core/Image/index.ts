import { usage } from './Image.demo.usage'
import { height } from './Image.demo.height'
import { contain } from './Image.demo.contain'
import { fallback } from './Image.demo.fallback'

export const ImageDemos = {
  usage,
  height,
  contain,
  fallback,
}
