import { usage } from './SegmentedControl.demo.usage'
import { generic } from './SegmentedControl.demo.generic'
import { disabled } from './SegmentedControl.demo.disabled'
import { labels } from './SegmentedControl.demo.labels'
import { configurator } from './SegmentedControl.demo.configurator'
import { autoContrast } from './SegmentedControl.demo.autoContrast'
import { transitions } from './SegmentedControl.demo.transitions'
import { readOnly } from './SegmentedControl.demo.readOnly'
import { stylesApi } from './SegmentedControl.demo.stylesApi'
import { iconsOnly } from './SegmentedControl.demo.iconsOnly'

export const SegmentedControlDemos = {
  usage,
  generic,
  disabled,
  labels,
  configurator,
  autoContrast,
  transitions,
  readOnly,
  stylesApi,
  iconsOnly,
}
