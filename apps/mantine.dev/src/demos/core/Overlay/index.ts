import { usage } from './Overlay.demo.usage'
import { gradient } from './Overlay.demo.gradient'
import { blur } from './Overlay.demo.blur'

export const OverlayDemos = {
  usage,
  gradient,
  blur,
}
