import { usage } from './MaskInput.demo.usage'
import { dynamic } from './MaskInput.demo.dynamic'
import { customTokens } from './MaskInput.demo.customTokens'
import { regex } from './MaskInput.demo.regex'
import { transform } from './MaskInput.demo.transform'
import { disabled } from './MaskInput.demo.disabled'
import { error } from './MaskInput.demo.error'
import { resetRef } from './MaskInput.demo.resetRef'
import { withUseForm } from './MaskInput.demo.withUseForm'

export const MaskInputDemos = {
  usage,
  dynamic,
  customTokens,
  regex,
  transform,
  disabled,
  error,
  resetRef,
  withUseForm,
}
