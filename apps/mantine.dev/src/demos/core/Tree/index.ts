import { usage } from './Tree.demo.usage'
import { controller } from './Tree.demo.controller'
import { checked } from './Tree.demo.checked'
import { checkAllNodes } from './Tree.demo.checkAllNodes'
import { expandedState } from './Tree.demo.expandedState'
import { asyncLoading } from './Tree.demo.asyncLoading'
import { searchFilter } from './Tree.demo.searchFilter'

export const TreeDemos = {
  usage,
  controller,
  checked,
  checkAllNodes,
  expandedState,
  asyncLoading,
  searchFilter,
}
