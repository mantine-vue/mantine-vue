import { usage } from './RingProgress.demo.usage'
import { configurator } from './RingProgress.demo.configurator'
import { rootColor } from './RingProgress.demo.rootColor'
import { sectionGap } from './RingProgress.demo.sectionGap'
import { startAngle } from './RingProgress.demo.startAngle'
import { sectionsProps } from './RingProgress.demo.sectionsProps'
import { label } from './RingProgress.demo.label'
import { transitions } from './RingProgress.demo.transitions'

export const RingProgressDemos = {
  usage,
  configurator,
  rootColor,
  sectionGap,
  startAngle,
  sectionsProps,
  label,
  transitions,
}
