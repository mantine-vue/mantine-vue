import { usage } from './Anchor.demo.usage'
import { decoration } from './Anchor.demo.decoration'
import { textProps } from './Anchor.demo.textProps'

export const AnchorDemos = {
  usage,
  decoration,
  textProps,
}
