import { usage } from './RollingNumber.demo.usage'
import { prefix } from './RollingNumber.demo.prefix'
import { duration } from './RollingNumber.demo.duration'

export const RollingNumberDemos = {
  usage,
  prefix,
  duration,
}
