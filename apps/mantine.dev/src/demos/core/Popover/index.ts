import { usage } from './Popover.demo.usage'
import { hover } from './Popover.demo.hover'
import { form } from './Popover.demo.form'
import { inline } from './Popover.demo.inline'
import { sameWidth } from './Popover.demo.sameWidth'
import { offset } from './Popover.demo.offset'
import { arrow } from './Popover.demo.arrow'
import { overlay } from './Popover.demo.overlay'
import { disabled } from './Popover.demo.disabled'
import { clickOutsideEvents } from './Popover.demo.clickOutsideEvents'

export const PopoverDemos = {
  usage,
  hover,
  form,
  inline,
  sameWidth,
  offset,
  arrow,
  overlay,
  disabled,
  clickOutsideEvents,
}
