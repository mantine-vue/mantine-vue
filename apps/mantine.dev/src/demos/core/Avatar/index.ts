import { usage } from './Avatar.demo.usage'
import { initials } from './Avatar.demo.initials'
import { allowedColors } from './Avatar.demo.allowedColors'
import { placeholders } from './Avatar.demo.placeholders'
import { configurator } from './Avatar.demo.configurator'
import { group } from './Avatar.demo.group'
import { groupTooltip } from './Avatar.demo.groupTooltip'
import { link } from './Avatar.demo.link'

export const AvatarDemos = {
  usage,
  initials,
  allowedColors,
  placeholders,
  configurator,
  group,
  groupTooltip,
  link,
}
