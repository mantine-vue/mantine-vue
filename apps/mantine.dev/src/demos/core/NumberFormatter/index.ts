import { usage } from './NumberFormatter.demo.usage'
import { prefixSuffix } from './NumberFormatter.demo.prefixSuffix'
import { thousandsSeparator } from './NumberFormatter.demo.thousandsSeparator'
import { decimalScale } from './NumberFormatter.demo.decimalScale'

export const NumberFormatterDemos = {
  usage,
  prefixSuffix,
  thousandsSeparator,
  decimalScale,
}
