import { configurator } from './Loader.demo.configurator'
import { size } from './Loader.demo.size'
import { cssLoader } from './Loader.demo.cssLoader'
import { customType } from './Loader.demo.customType'

export const LoaderDemos = {
  configurator,
  size,
  cssLoader,
  customType,
}
