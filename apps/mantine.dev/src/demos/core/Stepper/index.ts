import { usage } from './Stepper.demo.usage'
import { allowStepSelect } from './Stepper.demo.allowStepSelect'
import { allowNextStepsSelect } from './Stepper.demo.allowNextStepsSelect'
import { configurator } from './Stepper.demo.configurator'
import { iconSizeConfigurator } from './Stepper.demo.iconSizeConfigurator'
import { icons } from './Stepper.demo.icons'
import { iconsOnly } from './Stepper.demo.iconsOnly'
import { stepColor } from './Stepper.demo.stepColor'
import { orientation } from './Stepper.demo.orientation'
import { iconPosition } from './Stepper.demo.iconPosition'
import { loading } from './Stepper.demo.loading'

export const StepperDemos = {
  usage,
  allowStepSelect,
  allowNextStepsSelect,
  configurator,
  iconSizeConfigurator,
  icons,
  iconsOnly,
  stepColor,
  orientation,
  iconPosition,
  loading,
}
