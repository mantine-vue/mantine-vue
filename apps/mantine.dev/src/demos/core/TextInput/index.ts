import { usage } from './TextInput.demo.usage'
import { loading } from './TextInput.demo.loading'
import { sections } from './TextInput.demo.sections'
import { error } from './TextInput.demo.error'
import { disabled } from './TextInput.demo.disabled'
import { stylesApi } from './TextInput.demo.stylesApi'

export const TextInputDemos = {
  usage,
  loading,
  sections,
  error,
  disabled,
  stylesApi,
}
