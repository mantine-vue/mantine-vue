import { usage } from './ThemeIcon.demo.usage'
import { gradient } from './ThemeIcon.demo.gradient'
import { autoContrast } from './ThemeIcon.demo.autoContrast'
import { variantColorsResolver } from './ThemeIcon.demo.variantColorsResolver'

export const ThemeIconDemos = {
  usage,
  gradient,
  autoContrast,
  variantColorsResolver,
}
