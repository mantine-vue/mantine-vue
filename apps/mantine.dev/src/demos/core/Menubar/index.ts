import { usage } from './Menubar.demo.usage'
import { hover } from './Menubar.demo.hover'
import { selectable } from './Menubar.demo.selectable'
import { controlled } from './Menubar.demo.controlled'
import { stylesApi } from './Menubar.demo.stylesApi'

export const MenubarDemos = {
  usage,
  hover,
  selectable,
  controlled,
  stylesApi,
}
