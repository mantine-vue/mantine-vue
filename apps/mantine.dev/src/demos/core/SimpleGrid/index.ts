import { usage } from './SimpleGrid.demo.usage'
import { responsive } from './SimpleGrid.demo.responsive'
import { container } from './SimpleGrid.demo.container'
import { minColWidth } from './SimpleGrid.demo.minColWidth'
import { autoFlow } from './SimpleGrid.demo.autoFlow'
import { autoRows } from './SimpleGrid.demo.autoRows'

export const SimpleGridDemos = {
  usage,
  responsive,
  container,
  minColWidth,
  autoFlow,
  autoRows,
}
