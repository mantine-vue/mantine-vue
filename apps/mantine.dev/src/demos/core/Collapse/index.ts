import { usage } from './Collapse.demo.usage'
import { horizontal } from './Collapse.demo.horizontal'
import { transition } from './Collapse.demo.transition'
import { nested } from './Collapse.demo.nested'

export const CollapseDemos = {
  usage,
  horizontal,
  transition,
  nested,
}
