import { usage } from './OverflowList.demo.usage'
import { maxRows } from './OverflowList.demo.maxRows'
import { maxVisibleItems } from './OverflowList.demo.maxVisibleItems'
import { collapseFrom } from './OverflowList.demo.collapseFrom'
import { hoverCard } from './OverflowList.demo.hoverCard'

export const OverflowListDemos = {
  usage,
  maxRows,
  maxVisibleItems,
  collapseFrom,
  hoverCard,
}
