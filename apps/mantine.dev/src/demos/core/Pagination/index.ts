import { configurator } from './Pagination.demo.configurator'
import { withContent } from './Pagination.demo.withContent'
import { siblings } from './Pagination.demo.siblings'
import { boundaries } from './Pagination.demo.boundaries'
import { responsive } from './Pagination.demo.responsive'
import { withPages } from './Pagination.demo.withPages'
import { composition } from './Pagination.demo.composition'
import { links } from './Pagination.demo.links'
import { icons } from './Pagination.demo.icons'
import { autoContrast } from './Pagination.demo.autoContrast'
import { size } from './Pagination.demo.size'
import { startValue } from './Pagination.demo.startValue'

export const PaginationDemos = {
  configurator,
  withContent,
  siblings,
  boundaries,
  responsive,
  withPages,
  composition,
  links,
  icons,
  autoContrast,
  size,
  startValue,
}
