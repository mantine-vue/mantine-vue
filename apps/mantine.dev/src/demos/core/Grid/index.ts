import { usage } from './Grid.demo.usage'
import { responsive } from './Grid.demo.responsive'
import { gap } from './Grid.demo.gap'
import { rowColumnGap } from './Grid.demo.rowColumnGap'
import { growConfigurator } from './Grid.demo.growConfigurator'
import { offset } from './Grid.demo.offset'
import { order } from './Grid.demo.order'
import { rows } from './Grid.demo.rows'
import { flexConfigurator } from './Grid.demo.flexConfigurator'
import { columnAlign } from './Grid.demo.columnAlign'
import { auto } from './Grid.demo.auto'
import { content } from './Grid.demo.content'
import { columns } from './Grid.demo.columns'
import { container } from './Grid.demo.container'

export const GridDemos = {
  usage,
  responsive,
  gap,
  rowColumnGap,
  growConfigurator,
  offset,
  order,
  rows,
  flexConfigurator,
  columnAlign,
  auto,
  content,
  columns,
  container,
}
