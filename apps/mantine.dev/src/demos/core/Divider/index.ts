import { usage } from './Divider.demo.usage'
import { variants } from './Divider.demo.variants'
import { label } from './Divider.demo.label'
import { sizes } from './Divider.demo.sizes'
import { vertical } from './Divider.demo.vertical'

export const DividerDemos = {
  usage,
  variants,
  label,
  sizes,
  vertical,
}
