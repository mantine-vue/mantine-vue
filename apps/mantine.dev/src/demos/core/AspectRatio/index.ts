import { image } from './AspectRatio.demo.image'
import { map } from './AspectRatio.demo.map'
import { video } from './AspectRatio.demo.video'
import { flex } from './AspectRatio.demo.flex'

export const AspectRatioDemos = {
  image,
  map,
  video,
  flex,
}
