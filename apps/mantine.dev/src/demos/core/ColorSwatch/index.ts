import { usage } from './ColorSwatch.demo.usage'
import { shadow } from './ColorSwatch.demo.shadow'
import { component } from './ColorSwatch.demo.component'

export const ColorSwatchDemos = {
  usage,
  shadow,
  component,
}
