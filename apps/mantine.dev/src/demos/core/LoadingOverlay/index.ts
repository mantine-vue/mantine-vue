import { usage } from './LoadingOverlay.demo.usage'
import { loaderProps } from './LoadingOverlay.demo.loaderProps'
import { customLoader } from './LoadingOverlay.demo.customLoader'

export const LoadingOverlayDemos = {
  usage,
  loaderProps,
  customLoader,
}
