import { usage } from './Timeline.demo.usage'
import { configurator } from './Timeline.demo.configurator'
import { bullet } from './Timeline.demo.bullet'

export const TimelineDemos = {
  usage,
  configurator,
  bullet,
}
