import { usage } from './SemiCircleProgress.demo.usage'
import { emptySegmentColor } from './SemiCircleProgress.demo.emptySegmentColor'
import { labelPosition } from './SemiCircleProgress.demo.labelPosition'
import { transitions } from './SemiCircleProgress.demo.transitions'

export const SemiCircleProgressDemos = {
  usage,
  emptySegmentColor,
  labelPosition,
  transitions,
}
