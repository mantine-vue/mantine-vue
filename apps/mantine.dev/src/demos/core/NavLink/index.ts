import { usage } from './NavLink.demo.usage'
import { active } from './NavLink.demo.active'
import { autoContrast } from './NavLink.demo.autoContrast'
import { nested } from './NavLink.demo.nested'

export const NavLinkDemos = {
  usage,
  active,
  autoContrast,
  nested,
}
