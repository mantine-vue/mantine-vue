import { usage } from './Scroller.demo.usage'
import { draggable } from './Scroller.demo.draggable'
import { scrollAmount } from './Scroller.demo.scrollAmount'
import { controlSize } from './Scroller.demo.controlSize'
import { customIcons } from './Scroller.demo.customIcons'

export const ScrollerDemos = {
  usage,
  draggable,
  scrollAmount,
  controlSize,
  customIcons,
}
