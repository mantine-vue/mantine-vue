import { usage } from './HoverCard.demo.usage'
import { delay } from './HoverCard.demo.delay'
import { group } from './HoverCard.demo.group'
import { profile } from './HoverCard.demo.profile'

export const HoverCardDemos = {
  usage,
  delay,
  group,
  profile,
}
