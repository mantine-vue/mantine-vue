import { usage } from './Title.demo.usage'
import { size } from './Title.demo.size'
import { textWrap } from './Title.demo.textWrap'
import { lineClamp } from './Title.demo.lineClamp'

export const TitleDemos = {
  usage,
  size,
  textWrap,
  lineClamp,
}
