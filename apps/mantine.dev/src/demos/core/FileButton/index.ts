import { usage } from './FileButton.demo.usage'
import { multiple } from './FileButton.demo.multiple'
import { reset } from './FileButton.demo.reset'

export const FileButtonDemos = {
  usage,
  multiple,
  reset,
}
