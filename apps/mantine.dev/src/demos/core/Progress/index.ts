import { usage } from './Progress.demo.usage'
import { compound } from './Progress.demo.compound'
import { vertical } from './Progress.demo.vertical'
import { transition } from './Progress.demo.transition'

export const ProgressDemos = {
  usage,
  compound,
  vertical,
  transition,
}
