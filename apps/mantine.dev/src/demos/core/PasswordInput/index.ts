import { usage } from './PasswordInput.demo.usage'
import { loading } from './PasswordInput.demo.loading'
import { controlledVisibility } from './PasswordInput.demo.controlledVisibility'
import { visibilityIcon } from './PasswordInput.demo.visibilityIcon'
import { strengthMeter } from './PasswordInput.demo.strengthMeter'
import { sections } from './PasswordInput.demo.sections'
import { error } from './PasswordInput.demo.error'
import { disabled } from './PasswordInput.demo.disabled'
import { stylesApi } from './PasswordInput.demo.stylesApi'

export const PasswordInputDemos = {
  usage,
  loading,
  controlledVisibility,
  visibilityIcon,
  strengthMeter,
  sections,
  error,
  disabled,
  stylesApi,
}
