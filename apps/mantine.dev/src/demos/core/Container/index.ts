import { breakout } from './Container.demo.breakout'
import { usage } from './Container.demo.usage'
import { fluid } from './Container.demo.fluid'
import { sizes } from './Container.demo.sizes'
import { responsive } from './Container.demo.responsive'

export const ContainerDemos = {
  breakout,
  usage,
  fluid,
  sizes,
  responsive,
}
