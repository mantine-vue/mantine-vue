import { usage } from './AngleSlider.demo.usage'
import { formatLabel } from './AngleSlider.demo.formatLabel'
import { marks } from './AngleSlider.demo.marks'
import { onChangeEnd } from './AngleSlider.demo.onChangeEnd'
import { disabled } from './AngleSlider.demo.disabled'
import { radialMove } from './AngleSlider.demo.radialMove'

export const AngleSliderDemos = {
  usage,
  formatLabel,
  marks,
  onChangeEnd,
  disabled,
  radialMove,
}
