import { usage } from './ScrollArea.demo.usage'
import { horizontal } from './ScrollArea.demo.horizontal'
import { scrollbars } from './ScrollArea.demo.scrollbars'
import { scrollPosition } from './ScrollArea.demo.scrollPosition'
import { boundaries } from './ScrollArea.demo.boundaries'
import { scrollTo } from './ScrollArea.demo.scrollTo'
import { startScrollPosition } from './ScrollArea.demo.startScrollPosition'
import { scrollIntoView } from './ScrollArea.demo.scrollIntoView'
import { maxHeight } from './ScrollArea.demo.maxHeight'

export const ScrollAreaDemos = {
  usage,
  horizontal,
  scrollbars,
  scrollPosition,
  boundaries,
  scrollTo,
  startScrollPosition,
  scrollIntoView,
  maxHeight,
}
