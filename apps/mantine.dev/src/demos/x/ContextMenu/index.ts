import { usage, actions } from './ContextMenu.demo.usage'
import { multipleTargets, submenus } from './ContextMenu.demo.submenus'
import { customContent, imperative } from './ContextMenu.demo.advanced'
import { providerSettings, rtl, styling } from './ContextMenu.demo.styling'

export const ContextMenuDemos = {
  usage,
  actions,
  multipleTargets,
  submenus,
  customContent,
  imperative,
  styling,
  providerSettings,
  rtl,
}
