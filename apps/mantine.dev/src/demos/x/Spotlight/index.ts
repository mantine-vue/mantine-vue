import { usage } from './Spotlight.demo.usage'
import { limit } from './Spotlight.demo.limit'
import { scrollable } from './Spotlight.demo.scrollable'
import { groups } from './Spotlight.demo.groups'
import { fuzzySearch } from './Spotlight.demo.fuzzySearch'
import { compound } from './Spotlight.demo.compound'
import { customAction } from './Spotlight.demo.customAction'

export const SpotlightDemos = {
  usage,
  limit,
  scrollable,
  groups,
  fuzzySearch,
  compound,
  customAction,
}
